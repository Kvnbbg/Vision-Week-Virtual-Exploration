/// The line `import 'package:flutter/material.dart';` in the `settings_screen.dart` file is importing
/// the material library from the Flutter framework. This library provides widgets and utilities  for
/// building user interfaces following the Material Design guidelines.
import 'package:flutter/material.dart';

// settings_screen.dart
class EcranParametres extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Paramètres'),
    );
  }
}